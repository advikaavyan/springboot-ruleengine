package com.example.advikaavyan.adaptor.model.processor;

public class ContextBuilderProcessor {
  /*  public BaseContext messageToBaseContext(String message) {

        MessageDataDto messageDataDto = MessageDataDto.builder().rawMessage(message).build();
        TransactionCollection<Transaction> transactions = new TransactionCollectionImpl();
        transactions.appendTransaction()
        StpContext stpContext = StpContext.builder().build();
        Map<TransactionKey, TransactionCollection> transactionCollectionMap = stpContext.
                addTransactions(TransactionKey.valueOf(messageDataDto.getMessageFunction()),

        transactionCollectionMap.a
    }*/
}
